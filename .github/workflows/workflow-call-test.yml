name: A Test Workflow Call File.

on:
  workflow_call:

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Echo Hello World
        run: echo "hello world!"
